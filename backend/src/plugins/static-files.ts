import type { FastifyInstance } from 'fastify';

export async function staticFiles(_app: FastifyInstance) {
  // Placeholder for Story 5.3: Production Docker Build & Deployment
  // Will serve built frontend via @fastify/static
}
